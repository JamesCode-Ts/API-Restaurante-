
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect
server.servlet.context-path=/apirestaurante
#spring.profiles.active=${PROFILES_APP}

# Configuração para ficar em produção


#nÃºmero mÃ¡ximo de milissegundos que um cliente aguardarÃ¡ por uma conexÃ£o
 #spring.datasource.hikari.connection-timeout = 20000 

#nÃºmero mÃ­nimo de conexÃµes inativas mantidas pelo HikariCP em um conjunto de conexÃµes
# spring.datasource.hikari.minimum-idle= 10 

#MÃ¡ximo do pool de conexÃ£o
# #spring.datasource.hikari.maximum-pool-size= 40 

#Tempo ociosos para conexÃ£o
#spring.datasource.hikari.idle-timeout=10000 

#salvando dados no banco automaticamente
# spring.datasource.hikari.auto-commit =true 
